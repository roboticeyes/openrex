language: c
script:
    - mkdir build && cd build && cmake -DCMAKE_BUILD_TYPE=Debug .. && cmake --build . && ctest
addons:
    apt:
        packages:
            - libglew-dev
            - libsdl2-dev
            - check
